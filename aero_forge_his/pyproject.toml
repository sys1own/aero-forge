[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "aero_forge_his"
version = "0.1.0"
description = "Holographic Invariant Storage backend for Aero-Forge"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
