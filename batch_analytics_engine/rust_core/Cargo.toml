[package]
name = "batch_analytics_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "batch_analytics_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py310"] }
